changes in db(market):
1) changed table city to table address (need to routes on map in the future);
2) added stocks for producers and suppliers (id, owner, address);
3) products and materials now have availability_status;